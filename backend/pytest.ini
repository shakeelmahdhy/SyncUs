[pytest]
pythonpath = .
testpaths = tests
markers =
    smoke: fast critical-path checks for deployment sanity
    unit: isolated unit-level checks
    integration: cross-module API/service behavior checks
