# Optional. Leave unset in local dev — Vite proxies /jobs, /accounts, etc. to :8000.
# For production builds, set your deployed API origin (no trailing slash).
# VITE_API_BASE_URL=http://127.0.0.1:8000
